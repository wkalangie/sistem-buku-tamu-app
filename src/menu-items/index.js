import admin from './admin';
import staff from './staff';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [admin],
    staff: [staff],
    admin: [admin]
};

export default menuItems;
